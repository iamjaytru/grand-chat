export type LinkType = {
    id: string;
    image: string;
    href: string;
    title: string;
}

export type MessageType = {
    id: string;
    user_id: string;
    content: string;
}

export type UserType = {
    id: string;
    // name: string;
    avatar: string;
    // last_seen: string;
    // status: string;
    // is_online: boolean;
    // is_typing: boolean;
}